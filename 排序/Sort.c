#pragma once
#include "Sort.h"
//#include "ShellSort.h"
//#include "quicksort.h"

extern void PrintSort(int* a, int n);

int main()
{
	TestSort();

	//int arr[10] = { 4,7,3,5,1,2,8,0,9,6 };
	//int sz = sizeof(arr) / sizeof(arr[0]);
	//ShellSort(arr, sz);
	//Rise_ShellSort(arr, sz);
	//quicksortNonR(arr, 0, sz - 1);
	//PrintSort(arr, sz);
	return 0;
}